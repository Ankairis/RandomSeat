# 随机排座位原理

## 一、数据结构

$$grid[r][c] = \{ sel: bool,\ id: number \mid null \}$$

二维数组，每个格子存两个状态：是否选中、分配学号。

## 二、流程概述

1. 用户输入行数和列数，生成 $N \times M$ 的座位表
2. 默认所有格子选中（绿色），用户可点击切换选中/取消
3. 取消的格子（灰色）不参与学号分配
4. 选中 $M$ 个格子 → 自动生成学号列表 $1, 2, 3, ..., M$
5. 点击"随机安排" → Fisher-Yates 洗牌打乱学号 → 依次填入选中座位

## 三、核心算法：Fisher-Yates 洗牌

Fisher-Yates（也称 Knuth 洗牌算法）是生成随机排列的标准算法，保证每种排列等概率出现，即每种排列的概率均为 $1/n!$。

### 算法步骤

初始数组: $[1, 2, 3, 4, 5]$

---

**第 1 步:** $i = 4$（最后一个元素下标）

随机选取 $j \in [0, 4]$，假设 $j = 1$

交换 $arr[4]$ 和 $arr[1]$

结果: $[1, 5, 3, 4, 2]$

---

**第 2 步:** $i = 3$

随机选取 $j \in [0, 3]$，假设 $j = 0$

交换 $arr[3]$ 和 $arr[0]$

结果: $[4, 5, 3, 1, 2]$

---

**第 3 步:** $i = 2$

随机选取 $j \in [0, 2]$，假设 $j = 2$

交换 $arr[2]$ 和 $arr[2]$（自身交换，不变）

结果: $[4, 5, 3, 1, 2]$

---

**第 4 步:** $i = 1$

随机选取 $j \in [0, 1]$，假设 $j = 0$

交换 $arr[1]$ 和 $arr[0]$

结果: $[5, 4, 3, 1, 2]$

---

**最终排列:** $[5, 4, 3, 1, 2]$

### 伪代码

```
for i = n-1 down to 1:
    j ← random(0, i)    // 包含两端
    swap(arr[i], arr[j])
```

### 数学性质

- **时间复杂度**: $O(n)$
- **空间复杂度**: $O(n)$（需复制原数组）
- **均匀性**: 每种排列概率均为 $1/n!$，严格等概率
- **无模偏差**: 使用拒绝采样或无偏随机数生成器

### 正确性证明要点

算法保证每个元素出现在每个位置的概率为 $1/n$。归纳法证明：

当 $i = n-1$ 时，$arr[n-1]$ 被等概率选为 $arr[0..n-1]$ 中任意一个。

当 $i = k$ 时，前 $n-k-1$ 个位置已固定，剩余 $k+1$ 个元素中每个被选到位置 $k$ 的概率为 $1/(k+1)$。

## 四、学号范围

选中 $M$ 个格子 → 系统自动生成学号 $1$ 到 $M$。保证学号数量与选中座位数一致。

$$ids = [1, 2, 3, \ldots, M]$$

## 五、分配流程

1. 遍历 $grid$，收集所有 $sel = true$ 的格子坐标 → $selected[]$（长度 $M$）
2. 生成学号列表 $[1, 2, 3, \ldots, M]$
3. Fisher-Yates 洗牌打乱学号列表
4. 遍历 $selected[]$，按顺序将打乱后的学号填入格子
5. 渲染页面，显示分配结果

每次点击"随机安排"都会重新洗牌，每次结果独立。

## 六、示例

```
7×7  →  49 格全选中   →  学号 [1..49]
     ↓
取消 9 格             →  40 格选中
     ↓
随机安排              →  Fisher-Yates 打乱 1..40
     ↓
按顺序填入 40 个格子   →  分配完成
```
